from numpy import random
from src.models.map import Map
from numpy import random
import numpy as np
import csv

if __name__ == '__main__':

    for i in range(3):
        for j in range(3):
            print(str(i)+'-'+str(j)+'a')
        for j in range(3):
            print(str(i) + '-' + str(j) + 'b')
        for j in range(3):
            print(str(i) + '-' + str(j) + 'c')
